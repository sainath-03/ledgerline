# --- Database (Vercel Postgres) ---
# Created automatically when you add the "Postgres" storage integration
# in the Vercel dashboard (Project -> Storage -> Create Database -> Postgres).
# Vercel injects POSTGRES_URL for you in Production/Preview; for local dev,
# run `vercel env pull .env.local` after creating the database, or paste the
# connection string here.
POSTGRES_URL=

# --- Email (sent via your own Gmail account) ---
# 1. Turn on 2-Step Verification: https://myaccount.google.com/security
# 2. Create an App Password: https://myaccount.google.com/apppasswords
#    (choose app "Mail", device "Other" -> name it "Ledgerline")
# 3. Paste the 16-character app password below (no spaces needed either way).
GMAIL_USER=sainathreddy.mittu@gmail.com
GMAIL_APP_PASSWORD=

# Who the month-end summary gets sent to (usually the same as GMAIL_USER).
EMAIL_TO=sainathreddy.mittu@gmail.com

# --- Cron protection ---
# Any random string. Vercel Cron sends this back as a Bearer token so
# randoms on the internet can't trigger your email endpoint. Generate one
# with: openssl rand -hex 32
CRON_SECRET=
